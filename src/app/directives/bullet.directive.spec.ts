import { BulletDirective } from './bullet.directive';

describe('BulletDirective', () => {
  it('should create an instance', () => {
    const directive = new BulletDirective();
    expect(directive).toBeTruthy();
  });
});
