import { DestroyerDirective } from './destroyer.directive';

describe('DestroyerDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyerDirective();
    expect(directive).toBeTruthy();
  });
});
